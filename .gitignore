# Secrets — NEVER commit. Only *.example.json templates are tracked.
ops.config.json
ops.config.*.json
.env
.env.*
users.json
roles.json
admin-hide.json

# Per-user / shared operational state
ops-lists/
user-lists/

# Audit + run logs, watermarks
*-audit.log
*.log
.watermark

# Generated / local
data.js
erp-mock/

# Local Claude Code workspace (skills, scheduled-task lock) - keep local
.claude/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp
.vs/
